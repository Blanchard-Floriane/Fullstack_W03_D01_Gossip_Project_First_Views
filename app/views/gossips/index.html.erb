<div class="hero-color px-4 py-5 mb-5 text-center">
  <h1 class="display-5 fw-bold">Centered hero</h1>
  <div class="col-lg-6 mx-auto">
    <p class="lead mb-4">Quickly design and customize responsive mobile-first sites with Bootstrap, the world’s most popular front-end open source toolkit, featuring Sass variables and mixins, responsive grid system, extensive prebuilt components, and powerful JavaScript plugins.</p>
    <div class="d-grid gap-2 d-sm-flex justify-content-sm-center">
      <button type="button" class="btn btn-dark btn-lg px-4 gap-3">Share a Gossip</button>
    </div>
  </div>
</div>

<div class="row px-4 py-2">
  <% @gossips.each do |gossip|%> 
    <div class="col-sm-4 my-2">
      <div class="card">
        <div class="card-body">
          <h5 class="card-title"><%= gossip.title %></h5>
          <p class="card-text">Author: <%= gossip.user.first_name %></p>
          <div class="text-end">
            <%= link_to "See More", gossip_path(gossip.id), class:"btn btn-pink"%>
          </div>
        </div>
      </div>
    </div>
  <% end %>
</div>
